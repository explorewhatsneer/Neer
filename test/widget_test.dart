// Neer app smoke test placeholder
// TODO: Add proper widget tests for Neer screens
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder - app builds', () {
    // Supabase initialization requires real credentials,
    // so full widget tests need mock setup.
    expect(true, isTrue);
  });
}
